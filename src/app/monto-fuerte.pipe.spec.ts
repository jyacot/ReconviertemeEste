import { MontoFuertePipe } from './monto-fuerte.pipe';

describe('MontoFuertePipe', () => {
  it('create an instance', () => {
    const pipe = new MontoFuertePipe();
    expect(pipe).toBeTruthy();
  });
});
