import { MontoValidoPipe } from './monto-valido.pipe';

describe('MontoValidoPipe', () => {
  it('create an instance', () => {
    const pipe = new MontoValidoPipe();
    expect(pipe).toBeTruthy();
  });
});
