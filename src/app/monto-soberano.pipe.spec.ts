import { MontoSoberanoPipe } from './monto-soberano.pipe';

describe('MontoSoberanoPipe', () => {
  it('create an instance', () => {
    const pipe = new MontoSoberanoPipe();
    expect(pipe).toBeTruthy();
  });
});
